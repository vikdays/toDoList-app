<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Полупрозрачная тема для фрагмента -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Ваши другие настройки -->
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Это делает иконки статус-бара тёмными -->
    </style>

    <style name="CustomToolbarStyle" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">@color/bej</item> <!-- Цвет текста -->
    </style>
    <style name="CustomToolbarTitleStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">24sp</item> <!-- Увеличение размера текста -->
    </style>

</resources>
